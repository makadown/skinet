/**
 * Interface for address
 * contained in address.ts
 */
export interface IAddress {
    firstName: string;
    lastName: string;
    street: string;
    city: string;
    state: string;
    zipCode: string;
}